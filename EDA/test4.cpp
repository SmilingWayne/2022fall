#include <iostream>
#include <fstream>
#include <string>
#include <bitset>
using namespace std;

void test(double ans[20][20])
{
    cout << ans[0][0] << endl;
}

// struct
int main(int argc, char *argv[])
{
    double ans[20][20];
    // memset(ans, 0, sizeof(i) )
    ans[0][0] = 0;
    test(ans);

    // < t.a << " " << t.s << endl;
}
